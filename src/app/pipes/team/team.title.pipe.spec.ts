import { TeamTitlePipe } from './team.title.pipe';

describe('TeamTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TeamTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
